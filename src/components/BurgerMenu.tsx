import { useState } from 'react';
import { Link } from '@tanstack/react-router';
import { Menu, X, Plane, ArrowUpRight } from 'lucide-react';
import { Button } from './ui/button';
import { ServiceModal } from './ServiceModal';
import type { ServiceKey } from './ServicePills';
export function BurgerMenu(){const [open,setOpen]=useState(false);const [service,setService]=useState<{key:ServiceKey;label:string}|null>(null);return <><header className="portal-nav"><Link to="/" className="brand-lockup"><span className="brand-icon"><Plane/></span><span>AL-BAHR<small>TRAVELS & CONSULTANTS</small></span></Link><Button variant="ghost" size="icon" className="mobile-menu-toggle" aria-label={open?'Close menu':'Open menu'} aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</Button><nav className={open?'nav-links is-open':'nav-links'} aria-label="Main navigation"><Link to="/" onClick={()=>setOpen(false)}>Home</Link><Link to="/about" onClick={()=>setOpen(false)}>About Us</Link><Button variant="ghost" onClick={()=>{setService({key:'umrah',label:'Umrah Packages'});setOpen(false)}}>Umrah Packages</Button><Button variant="ghost" onClick={()=>{setService({key:'visa',label:'Visa Services'});setOpen(false)}}>Visa Services</Button><Link to="/b2b" onClick={()=>setOpen(false)}>B2B Sub-Agent Signup</Link><Button asChild><Link to="/contact" onClick={()=>setOpen(false)}>Contact Us <ArrowUpRight/></Link></Button></nav></header><ServiceModal service={service} onClose={()=>setService(null)}/></>}
